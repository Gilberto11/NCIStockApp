<div class ="jumbotron bg-dark text-white">
<h1>NCI Stock App</h1>
</div>
<div class ="jumbotron bg-dark text-white">
<h2> For more details of a company stock, please register or sign in </h2>
</div>

<!-- create a loop "do" then create a text field with tag ticket and place holder has the message for the user in the field, with a button called lookup-->
<%= form_tag root_path, :method => 'POST' do%>
	<%= text_field_tag 'ticker', nil, placeholder: "Enter Ticker Symbol", size: 50 %>
<%= submit_tag 'Lookup', class: 'btn btn-outline-secondary' %>
<% end %>

<br/>

<!-- if the field has a company symbol returns company name and pricer-->
<% if @stock %>
	<%= @stock.company_name %><br/>
	Latest Price: $<%= @stock.latest_price %><br/>
	Source: <%= @stock.latest_source %><br/>
	<%= @stock.attribution %><br/>
	 
<!-- else if statment in case the field is empty then return the message defined for the variable empty in the home controller-->
<% elsif @empty %>
	<%= @empty %>
<% end %>

<!-- validation for user entering invalid symbol-->
<!-- <% if @error %>
		<%= @error %>
	<% end %> -->
	